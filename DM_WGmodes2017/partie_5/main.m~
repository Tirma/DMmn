function main

% g = BT1; %discretisation du guide
% g = TE_solve(g,1);
% g.neff
% 
% Z = display_mode(g,1);
% figure
% surfc(g.x,g.y,Z);
% xlabel('x (µm)');
% ylabel('y (µm)');
% zlabel('Amplitude');

% g = BT2; %discretisation du guide
% g = TE_solve(g,1);
% g.neff
% 
% Z = display_mode(g,1);
% figure
% surfc(g.x,g.y,Z);
% xlabel('x (µm)');
% ylabel('y (µm)');
% zlabel('Amplitude');

g = BT3; %discretisation du guide
g = TE_solve(g,1);
g.neff

Z = display_mode(g,1);
figure
surfc(g.x,g.y,Z);
xlabel('x (µm)');
ylabel('y (µm)');
zlabel('Amplitude');

end